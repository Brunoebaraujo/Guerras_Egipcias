// @ts-check
/** Regras estruturais compartilhadas por UI, persistência e servidor. */
export const DECK_SIZE = 12;
export const LANES = Object.freeze([0, 1, 2]);
export const MAX_CARDS_PER_LANE = 4;
